# =====================================
# 資料探勘課程 .gitignore
# =====================================

# 資料集檔案 (可通過 data_download.py 重新下載)
# =====================================
datasets/raw/
datasets/processed/
data_mining_course/datasets/raw/
data_mining_course/datasets/processed/
# 下載工具的暫存（HF / KaggleHub），刻意放在課程樹下而非 ~/.cache，匯出完可刪
datasets/.hf_cache/
datasets/.kagglehub_cache/
data_mining_course/datasets/.hf_cache/
data_mining_course/datasets/.kagglehub_cache/
*.csv
# 例外：二手車原始資料隨 repo 附上——專案與總整兩本 notebook 都直接讀它，
# 且 Kaggle 需要帳號憑證，學生不該為了跑第一個範例先去申請 API token。
!data_mining_course/projects/project/car_data/*.csv
# 例外：UCI Automobile 資料集，legacy/ 的舊版視覺化 notebook 讀它（24 KB）
!data_mining_course/projects/project/Automobile_data.csv

# 投影片產出隨 repo 走：103 頁共 81 MB，檔名固定為 pNN.png，
# 重生是覆蓋而不是新增，diff 乾淨。用 build_slides.py --all 重建。
# 聯絡表是衍生物，幾秒就能由 make_contact_sheet.py 重建，不進版控。
data_mining_course/slides_plan/_contact/
*.json
*.xlsx
*.xls
*.parquet
*.h5
*.hdf5
*.pkl
*.pickle

# 大型資料檔案
# =====================================
*.zip
*.tar.gz
*.rar
*.7z
*.gz
*.bz2

# 圖片和音訊檔案 (通常是資料集的一部分)
# =====================================
*.jpg
*.jpeg
*.png
*.gif
*.bmp
*.tiff
*.svg
*.wav
*.mp3
*.mp4
*.avi
*.mov
*.flv

# Python 相關
# =====================================
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# 虛擬環境
# =====================================
venv/
env/
ENV/
env.bak/
venv.bak/
.venv/
conda-env/
.conda/

# PyTorch
# =====================================
*.pth
*.pt
checkpoints/
runs/
lightning_logs/

# TensorFlow
# =====================================
*.pb
*.h5
*.hdf5
saved_model/
logs/
tensorboard_logs/

# Jupyter Notebook
# =====================================
.ipynb_checkpoints/
*/.ipynb_checkpoints/*
*.nbconvert.py
*.ipynb_backup

# IPython
# =====================================
profile_default/
ipython_config.py

# 環境變數和配置檔案
# =====================================
.env
.env.local
.env.*.local
config.ini
secrets.json
credentials.json

# Kaggle API 憑證 (敏感資訊)
# =====================================
kaggle.json
.kaggle/

# 作業系統產生的檔案
# =====================================
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# Office 暫存鎖檔（開啟 .docx/.xlsx/.pptx 時產生）
~$*

# IDE 和編輯器檔案
# =====================================
.vscode/settings.json
.vscode/launch.json
.vscode/tasks.json
.vscode/.ropeproject
.idea/
*.swp
*.swo
*~

# 快取和暫存檔案
# =====================================
.cache/
.pytest_cache/
.coverage
.tox/
.nox/
htmlcov/
.mypy_cache/
.dmypy.json
dmypy.json

# 模型檔案和權重 (通常很大)
# =====================================
*.model
*.weights
*.bin
models/
weights/
checkpoints/

# 實驗和結果檔案
# =====================================
experiments/
results/
outputs/
plots/
figures/
*.log
*.out

# 暫存和備份檔案
# =====================================
*.tmp
*.temp
*.bak
*.backup
*~
.#*
#*#

# 壓縮檔案
# =====================================
*.tar
*.zip
*.gz

# 剪貼簿記錄 (個人記錄檔案)
# =====================================
*.Clipboard_Record.md
.Clipboard_Record.md

# 範例：如果要保留特定的小型範例資料集，可以使用 !
# =====================================
# !datasets/raw/example/small_sample.csv

# 註解：
# - 原始資料集和處理後資料集由於檔案過大且可重新下載，不納入版本控制
# - 使用 data_download.py 可以重新下載所有 Kaggle 資料集
# - 個人的 Kaggle API 憑證檔案包含敏感資訊，絕對不能上傳
# - 模型檔案和實驗結果通常檔案很大，建議使用 Git LFS 或其他方式管理

# README 行銷視覺資產（Hero / 社群圖 / GIF / 底圖 / 生成腳本）
# 覆蓋上方 *.png / *.gif 等萬用忽略，讓 assets/ 隨 repo 一起發佈
!assets/
!assets/**

# 概念示意圖（嵌入教學 notebook 用，需隨 repo 一起發佈）
# =====================================
# 覆蓋上方 *.png 萬用忽略：只把各 notebook 的 concept_images/ 圖檔重新納入版控
!**/concept_images/
!**/concept_images/*.png

# 投影片產出（各章 slides_plan/*/_generated/），隨 repo 一起發佈
# 檔名固定為 pNN.png，重生是覆蓋而不是新增，diff 乾淨
!**/slides_plan/*/_generated/
!**/slides_plan/*/_generated/*.png
